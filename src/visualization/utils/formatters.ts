/**
 * Visualization formatters - re-export from shared module
 * This file exists for backward compatibility with visualization components
 */

export {
  formatDuration,
  formatNumber,
  formatPercentage,
  getFileIcon,
  getFileName,
  getDirectory,
  formatKindName,
  formatEventCount,
} from '../../shared/formatters';
